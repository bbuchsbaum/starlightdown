# The former copy/regex sync and MDX-marker machinery that lived here was
# removed. The replacement compiler pipeline lives in the sd_* modules
# (package-model.R, rd-render.R, sync.R, validate.R, ...) as they land.
