% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_site.R
\name{build_site}
\alias{build_site}
\title{Build the Starlight documentation site for a package}
\usage{
build_site(
  path = ".",
  site_dir = "starlight",
  articles = TRUE,
  examples = TRUE,
  run_dont_run = FALSE,
  lazy = TRUE,
  theme = NULL,
  npm_build = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{path}{Package root directory.}

\item{site_dir}{Starlight project directory, relative to \code{path}.}

\item{articles}{Build vignettes/articles (requires Quarto)?}

\item{examples}{Run examples in reference topics?}

\item{run_dont_run}{Run \verb{\dontrun{}} example blocks?}

\item{lazy}{Reuse cached article renders when sources are unchanged?}

\item{theme}{Site theme: \code{"default"} for the bundled Editorial Scientific
theme, or \code{"nova"} for that preset. \code{NULL} reads
\verb{starlightdown: theme:} from \verb{_pkgdown.yml}.}

\item{npm_build}{If \code{TRUE}, run \verb{npm run build} after generating content.}

\item{quiet}{Suppress progress messages?}
}
\value{
Invisibly, the path to \code{site_dir}.
}
\description{
Compiles package documentation (reference topics, articles, README, NEWS,
citation) into the Starlight content collection at
\verb{<site_dir>/src/content/docs/}, along with a typed \code{site.json} manifest
consumed by the bundled Starlight plugin. Output is staged, validated, and
committed atomically: a failed build never touches the live site, and
removed source pages cannot survive as stale output.
}
\examples{
\dontrun{
build_site()
}
}
