# testpkg.full 0.2.1 (2026-08-13)

## Bug fixes

- `full_add()` no longer drops names from `x`.
- `full_config()` keeps unrecognised options instead of discarding them.

# testpkg.full 0.2.0

## New features

- `full_plot()` gained an `n` argument.
- Added `full_describe()`, an S3 generic, with a default method.

## Breaking changes

- `full_experimental()` is marked experimental and may change.
