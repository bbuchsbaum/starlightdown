# Vision

Create beautiful, modern documentation sites for R packages by combining:
- `{altdoc}` for Markdown output from package sources.
- `{pkgdown}` for structured navigation and grouping.
- Astro Starlight for fast, polished presentation.

The goal is a smooth workflow where R developers run a single helper to render their docs, sync them into a Starlight site, and publish with minimal friction.
